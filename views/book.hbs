{{!-- <div class="container mt-2"> 
  {{#each categories}}
  
    <div class="card mb-3" onload="myFunction()">
        <div class="row no-gutters">
          <div class="col-md-4">
            <img src="/images/accomodations/{{this.image}}" class="card-img" style="height: 169px;">
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title">{{this.category}}</h5>
              <p class="card-text" id="desc">{{this.description}}</p>
              <p class="card-text"><small class="text-muted">Available Rooms: {{this.value}}</small></p>
            </div>
          </div>
        </div>
      </div>
    {{!-- slicing --}}
    
  {{/each}}
    
    
</div>
<!-- Central Modal Small -->
<div class="modal fade" id="centralModalSm" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
  aria-hidden="true">

  <!-- Change class .modal-sm to change the size of the modal -->
  <div class="modal-dialog modal-lg" role="document">


    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title w-100" id="myModalLabel">Modal title</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <img src="" id="category-image" class="img-fluid"/>
        <p id="description"></p>
      </div>
      <div class="modal-footer">
        <input type="submit" class="btn btn-success btn-md" value="Book Now">
        <button type="button" class="btn btn-danger btn-md" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
<!-- Central Modal Small -->

<script>
    /* must apply only after HTML has loaded */
    $(document).ready(function () {
        $('#centralModalSm').on('show.bs.modal', function(e) {
            var categoryName = $(e.relatedTarget).data('category')
            var image = $(e.relatedTarget).data('image')
            
            var value = $(e.relatedTarget).data('value')
            var description = $(e.relatedTarget).data('description')
            categoryName = categoryName.toUpperCase()
            console.log(description)
            $('#centralModalSm #myModalLabel').html(categoryName)
            $('.modal-body img').attr('src', '/images/accomodations/'+image)
            $('.modal-body #description').html(description)
        });
    });
  // slicing
      
</script> --}}